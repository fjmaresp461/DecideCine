<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".main.OptionsFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageButton
            android:id="@+id/ib_list"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="1dp"
            android:background="@drawable/button"
            android:backgroundTint="#ABFFFFFF"
            android:padding="10dp"
            android:src="@drawable/list_icon"
            app:layout_constraintEnd_toStartOf="@+id/ib_users"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/ib_users"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@drawable/button"
            android:backgroundTint="#00FFFFFF"
            android:padding="10dp"
            android:src="@drawable/users_icon"
            app:layout_constraintEnd_toStartOf="@+id/ib_settings"
            app:layout_constraintStart_toEndOf="@+id/ib_list"
            app:layout_constraintTop_toTopOf="@+id/ib_list" />

        <ImageButton
            android:id="@+id/ib_settings"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="3dp"
            android:background="@drawable/button"
            android:backgroundTint="#00FFFFFF"
            android:padding="10dp"
            android:src="@drawable/settings_icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/ib_users"
            app:layout_constraintTop_toTopOf="@+id/ib_users" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>